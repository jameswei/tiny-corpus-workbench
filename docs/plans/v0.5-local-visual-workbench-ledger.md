# Milestone v0.5 Task Ledger

**Status:** Active  
**Branch:** `milestone/v0.5-local-visual-workbench`  
**Approved base:** `6545636f1ded597a0850ef35705ef0137ac8ea38`  
**Accepted plan:** `docs/plans/v0.5-local-visual-workbench.md`  
**Frozen-plan SHA-256:** `1fd21d20b5aab1389ddeb6b3ddb4d489b5c5489a6b79d1664671f729d3d46e89`

This ledger records task state, review base, validation evidence, independent
review verdict, and checkpoint commit. Valid task states are `pending`,
`building`, `reviewing`, `accepted`, and `blocked`.

No later task starts until the current task has fresh builder completion, a
fresh independent reviewer `PASS`, validation on the unchanged reviewed delta,
review of tracked generated output, and a stable checkpoint commit.

| Task | State | Review base | Validation evidence | Verdict | Checkpoint |
| --- | --- | --- | --- | --- | --- |
| V05-1 Unified schema baseline | accepted | `ceaabc9` | Builder, fresh reviewer, and root rerun: 9 schema, 24 baseline, 3 preimage, and 6 provenance tests passed; compileall, JSON parsing, and diff check passed | `PASS` | `b12c2b4` |
| V05-2 Observation contracts and base fixtures | accepted | `41347ca` | Builder, fresh reviewer, and root rerun: 65 interaction, 30 observation, 8 provenance, 2 golden integration, and compatibility tests; lock, fixtures, compileall, and diff check passed | `PASS` | `89326eb` |
| V05-3 Diagnosis and refinement contracts | accepted | `92bdef4` | Builder, fresh reviewers, and root rerun: 63 focused, 65 V05-2 interaction, 15 bootstrap/local-integrity, and 5 integration tests; generators/fixtures, portability, lock, compileall, and diff checks passed | `PASS` | `e7b63f0` |
| V05-4 Corpus contracts and unified reset | accepted | `d0e15e5` | Builder, fresh reviewer, and root rerun: 224 units, 6 exact integrations, and reviewer 236 whole-tree tests passed; generators, fixtures, corpus specs, baseline audit, site, provenance, portability, compileall, and diff checks passed | `PASS` | `130f949` |
| V05-5 Admission, expansion, and projection | accepted | `9fabe40` | Builder, fresh reviewers, and root rerun: 66 focused and 287 whole-unit tests passed; workflow integration, raw-path probes, baseline audit, compileall, examples/workflows, and diff checks passed | `PASS` | `dfbef4b` |
| V05-6 Loopback server and CLI | accepted | `7a9543e` | Builder, fresh reviewers, and root rerun: 22 focused, 25 backing, and 310 whole-unit tests passed; immutable artifact responses, interrupt lifecycle, package inventory, foreground smoke, audit, compile, and diff checks passed | `PASS` | `917a6f1` |
| V05-7 Accessible visual workbench | accepted | `b7506dd` | Builder, fresh reviewers, and root rerun: validator, 11 UI tests, 8 integration tests, and 321 whole-unit tests passed; schema audit, compile, diff, package inventory, and real-browser accessibility/security smoke passed | `PASS` | `248906c` |
| V05-8 Documentation, CI, and release candidate | pending | — | — | — | — |

## Activity

- 2026-07-26: The project owner approved the frozen v0.5 plan.
- 2026-07-26: The frozen source hash and approved base were verified before
  milestone activation.
- 2026-07-26: Milestone setup was checkpointed at `ceaabc9`. V05-1 entered
  `building`.
- 2026-07-26: The fresh builder completed V05-1 with all required validation
  green. The unchanged implementation delta entered independent review against
  `ceaabc9`.
- 2026-07-26: Independent review returned `CHANGES_REQUIRED`. Blocking findings
  cover D006 and cross-field semantic negatives, canonical example coverage and
  identity coherence, accepted-base evidence inventory completeness, and
  monotonic provenance-registry enforcement. V05-1 returned to `building`.
- 2026-07-26: The builder closed all five findings and reran the complete V05-1
  validation gate. The corrected unchanged delta entered fresh independent
  review against `ceaabc9`.
- 2026-07-26: Fresh review confirmed D006 and monotonic-registry corrections,
  then returned `CHANGES_REQUIRED` for remaining API semantic negatives and an
  inventory that was still selective rather than exhaustive. V05-1 returned to
  `building`.
- 2026-07-26: The builder added 21 reviewer-specific and symmetric semantic
  negatives and made the accepted-base inventory mechanically exhaustive with
  149 migration-field entries. The final corrected delta entered another fresh
  independent review against `ceaabc9`.
- 2026-07-26: The next fresh review found remaining relation/evaluation,
  projection, artifact-union, corpus-summary, and inventory-exhaustiveness
  mismatches. V05-1 returned to `building`.
- 2026-07-26: The builder closed those findings, including valid nullable
  containment and same-role/different-path positives, and expanded the
  inventory to 180 migration-field entries. The unchanged delta entered fresh
  independent review again.
- 2026-07-26: Acceptance review found three remaining semantic gaps in
  kind-specific root descriptors, edge-source ownership, and the valid
  diagnosis-over-refinement relation. V05-1 returned to `building`.
- 2026-07-26: The builder closed all three gaps, added the canonical
  diagnosis-over-refinement branch, and reran the full gate. The unchanged
  delta entered fresh acceptance review.
- 2026-07-26: Fresh acceptance review returned `PASS`. Root validation reran on
  the unchanged reviewed delta and passed. V05-1 was checkpointed at
  `b12c2b4`.
- 2026-07-26: V05-2 entered `building` from clean accepted base `41347ca`.
- 2026-07-26: The fresh builder completed V05-2 with deterministic fixture
  regeneration and the full validation gate green. The unchanged delta entered
  independent review against `41347ca`.
- 2026-07-26: Independent review found stale active observation tests that
  still loaded v0.1 schemas. V05-2 returned to `building`; v0.1 remains allowed
  only as an explicit unsupported-input negative.
- 2026-07-26: The builder migrated the stale tests to v0.5 and passed the
  64-test interaction set. The corrected unchanged delta entered fresh review.
- 2026-07-26: Fresh review found one active-runtime boundary gap: missing or
  invalid bundled provenance registries were not always normalized to sanitized
  exit `6`. V05-2 returned to `building`.
- 2026-07-26: The builder normalized all bundled-registry failures and added
  eight exact-stream cases across `observe` and `verify`. The unchanged delta
  entered fresh final review.
- 2026-07-26: Fresh final review returned `PASS`. Root validation reran on the
  unchanged reviewed delta and passed. V05-2 was checkpointed at `89326eb`.
- 2026-07-26: V05-3 entered `building` from clean accepted base `92bdef4`.
- 2026-07-26: The fresh builder completed V05-3 with approved, rejected, and
  chained workflows plus all validation gates green. The unchanged delta
  entered independent review against `92bdef4`.
- 2026-07-26: Independent review found incomplete diagnosis/finding replay
  gating, unchecked chained-parent references, swallowed supplied-record
  provenance failures, and lost applicable regression coverage. V05-3 returned
  to `building`.
- 2026-07-26: The builder closed all four findings and mechanically classified
  all 40 prior diagnosis tests as 31 restored, 8 moved, and 1 obsolete. The
  corrected unchanged delta entered fresh review.
- 2026-07-26: Fresh review found missing partial-observation coverage behind two
  claimed moved tests and a pre-existing V05-2 bootstrap/runtime failure
  contract that must be corrected before checkpoint. V05-3 returned to
  `building`; corpus-only failures remain deferred to V05-4.
- 2026-07-26: The builder restored real partial-observation workflows, proved
  exact named test mappings, and fixed bootstrap/local-integrity coverage. The
  unchanged delta entered fresh review; full discovery now fails only in the
  16 V05-4-owned corpus cases.
- 2026-07-26: Fresh review returned `PASS`. A staged-output audit then added
  narrow binary attributes for the new PDF and DOCX fixtures; fresh review,
  checkout portability, cached diff hygiene, and root validation passed.
  V05-3 was checkpointed at `e7b63f0`.
- 2026-07-26: V05-4 entered `building` from clean accepted base `d0e15e5`.
- 2026-07-26: The fresh builder completed the corpus migration and unified
  reset with whole-tree validation and the deterministic audit green. The
  unchanged delta entered independent review against `d0e15e5`.
- 2026-07-26: Independent review found missing nested content-hash and
  member-error tuple binding, incomplete standalone audit checks, and one stale
  repository metadata path. V05-4 returned to `building`.
- 2026-07-26: The builder closed all findings, added adversarial audit tests,
  and passed whole-tree validation. The corrected unchanged delta entered fresh
  independent review.
- 2026-07-26: Fresh review returned `PASS` after independent real-corpus,
  adversarial-audit, portability, and whole-tree validation. Root reran the
  accepted gates on the unchanged reviewed delta, corrected one staged EOF
  whitespace defect, reran its focused checks, and checkpointed V05-4 at
  `130f949`.
- 2026-07-26: V05-5 entered `building` from clean accepted base `9fabe40`.
- 2026-07-26: The fresh builder completed admission, bounded corpus expansion,
  canonical-backing deduplication, relationship evaluation, and deterministic
  projection/detail serialization. The unchanged delta entered independent
  review against `9fabe40`.
- 2026-07-26: Independent review reproduced post-admission detail mutation,
  undeclared artifact authorization, chained-refinement projection failure,
  incomplete relation binding, premature record-key computation, and missing
  adversarial coverage. V05-5 returned to `building`.
- 2026-07-26: The builder closed all findings with frozen artifact evidence,
  exact authorization, post-equivalence keys, independent relation evaluation,
  complete refinement chains, and an expanded adversarial matrix. The corrected
  unchanged delta entered fresh independent review.
- 2026-07-26: Fresh review confirmed the prior fixes but reproduced mutable
  live-path reads during diagnosis/refinement relationship replay. V05-5
  returned to `building` for frozen-snapshot replay and race regressions.
- 2026-07-26: The builder moved all relation replay to private snapshots made
  only from admitted authorized bytes and added five replay contexts across
  content, path-replacement, and symlink mutations. The corrected unchanged
  delta entered another fresh independent review.
- 2026-07-26: Fresh review confirmed frozen replay but reproduced manifest and
  artifact substitutions during admission capture. V05-5 returned to
  `building` for race-resistant non-following capture and ancestor stability
  checks.
- 2026-07-26: The builder replaced admission pathname rereads with two complete
  non-following FD captures around intrinsic verification, added deterministic
  file/directory/root substitution regressions, and passed the expanded gates.
  The corrected unchanged delta entered fresh independent review.
- 2026-07-26: Fresh review reproduced an ABA swap in which intrinsic verification
  inspected different bytes than the accepted capture, plus false rejection
  from shared-ancestor link-count churn. V05-5 returned to `building` to bind
  verification to an immutable full capture and narrow ancestor witnesses.
- 2026-07-27: The builder moved intrinsic verification to a private snapshot
  made from the first complete capture, narrowed shared-ancestor witnesses to
  stable identity fields, and passed the expanded ABA/restoration/churn gates.
  The corrected unchanged delta entered fresh independent review.
- 2026-07-27: Fresh review confirmed the capture fixes but reproduced admission
  through a symlinked parent component hidden by early path resolution. V05-5
  returned to `building` for original-path no-follow traversal.
- 2026-07-27: The builder added no-follow validation of the exact caller path
  before both canonicalization points, covered parent/nested/final aliases, and
  passed the expanded gates. The corrected unchanged delta entered fresh
  independent review.
- 2026-07-27: Fresh review confirmed the no-follow walks but reproduced a
  canceled-alias path admitted after pre-walk `abspath` removed `alias/..`.
  V05-5 returned to `building` to preserve and validate the caller spelling.
- 2026-07-27: The builder removed pre-walk path normalization, rejected raw
  `.`/`..` components for single and batch admission, and passed the expanded
  canceled-alias gates. The corrected unchanged delta entered fresh independent
  review.
- 2026-07-27: Fresh review returned `PASS` after independent raw-path, alias,
  duplicate-root, replacement, immutable-capture, and full-suite validation.
  Root reran all accepted V05-5 gates on the unchanged reviewed delta and
  checkpointed V05-5 at `dfbef4b`.
- 2026-07-27: V05-6 entered `building` from clean accepted base `7a9543e`.
- 2026-07-27: The fresh builder completed the foreground loopback CLI/server,
  ordered request validation, authorized routes, safe headers, and bundled
  application shell. The unchanged delta entered independent review against
  `7a9543e`.
- 2026-07-27: Independent review reproduced replacement between artifact
  recheck and response bytes, plus `KeyboardInterrupt` escaping during browser
  opening without guaranteed socket closure. V05-6 returned to `building`.
- 2026-07-27: The builder bound artifact revalidation to immutable captured
  bytes and wrapped READY/browser/serve in one interrupt-safe lifecycle with
  unconditional closure. The corrected unchanged delta entered fresh review.
- 2026-07-27: Fresh review returned `PASS` after independent artifact mutation,
  interrupt lifecycle, request-precedence, package, full-suite, and foreground
  validation. Root reran the accepted gates on the unchanged reviewed delta and
  checkpointed V05-6 at `917a6f1`.
- 2026-07-27: V05-7 entered `building` from clean accepted base `b7506dd`.
- 2026-07-27: The fresh builder completed the accessible read-only workbench,
  explicit artifact text retrieval, offline asset validation, UI contract
  tests, and real-browser accessibility/security smoke. The unchanged delta
  entered fresh independent review against `b7506dd`.
- 2026-07-27: Independent review passed the complete test, package, browser,
  accessibility, and security gates but found that the corpus comparison view
  exposed only character counts and normalized equality. V05-7 returned to
  `building` to show all ten extractor metrics and exact deltas with focused
  regression coverage.
- 2026-07-27: The builder added the exact ten corpus comparison metrics,
  Docling and MarkItDown values, signed deltas, and separate normalized
  equality with focused UI and live-corpus regressions. The corrected unchanged
  delta entered a new fresh independent review.
- 2026-07-27: Fresh independent review returned `PASS` after exact ten-metric,
  signed-delta, incomplete-state, hostile-content, accessibility, responsive,
  package, audit, integration, and 321-test validation. Root repeated the
  accepted focused, browser, audit, package, compile, and diff gates on the
  unchanged reviewed delta and checkpointed V05-7 at `248906c`.
